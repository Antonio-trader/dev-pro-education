
pipeline {
    agent none

    stages {
        stage ('delete terraform') {
        agent {
            label 'Trinity'
        }
        steps {
            sh 'cd /root/git_repo/HW_dev_pro/education_terraform/blue-green && terraform destroy -auto-approve'
            }
        }
    }
}